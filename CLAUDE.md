# Contexto fijo del repo

Proyecto: El Laberinto Mágico — juego multijugador por turnos.
Stack: Angular (frontend/) + ASP.NET Core 10 / SignalR (backend/).
Arquitectura: Clean Architecture (Domain → Application → Infrastructure → Api).

## Regla de identidad de commits
- user.name y user.email ya están configurados localmente para este repo.
- NUNCA usar "Claude", "Claude Code", "AI Assistant" ni Co-authored-by de
  ninguna IA en mensajes de commit.
- Antes de cualquier commit, confirmar que la identidad configurada sigue
  siendo la del usuario humano (git config user.name / user.email).
